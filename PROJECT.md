# Community Site — Project Brain

> Единая точка истины. Любой агент начинает работу с этого файла.
> Последнее обновление: 2026-02-20

---

## 1. Владелец и контекст

**Никита Ефимов** — AI-архитектор, AI-студия Yes Workflow
- Telegram-канал: 460 подписчиков (закрытые комменты, только посты)
- YouTube: 1400 подписчиков
- Аудитория: обычные люди без технического фона, боятся AI
- Воронка: Shorts → бесплатный Telegram → **платное сообщество (этот сайт)**
- Предзаявок на платное сообщество: ~20 (без объявления цены)
- Живёт в Испании, аудитория в основном из России

**Продукты уже готовы (контент для сайта):**
- Курс по ChatGPT (записан)
- Курс по n8n (записан)

**Бренд сайта:** Никита Ефимов | AI Комьюнити
**Основной сайт:** yesworkflow.com (тёмная тема, cyan/teal акцент)

---

## 2. Видение продукта

Сайт-хаб для двух аудиторий:

| Аудитория | Что видят | Цель |
|---|---|---|
| **Все** (бесплатно) | Шаблоны N8N, скиллы для Claude Code, промты, базовые гайды | Привлечь, показать ценность, конвертировать |
| **Members** (платно) | Всё выше + курсы, записи эфиров, онбординг-программа | Удержать, обучить, создать сообщество |

**Главная ценность для members:** структурированная программа обучения (по дням/неделям) + живые эфиры + записи.

**Аналог по UX:** Skool — но своё, без зависимости от платформы.
**Визуальный референс:** aizdec.me (структура), yesworkflow.com (дизайн).

---

## 3. Финальный технический стек

| Компонент | Решение | Обоснование |
|---|---|---|
| Frontend | **Next.js 14+ (App Router)** | SSR, защищённые роуты, Vercel |
| База данных | **Supabase (PostgreSQL + RLS)** | Знакомый стек, контроль доступа |
| Стили | **Tailwind CSS + shadcn/ui** | Скорость разработки, красиво |
| Деплой | **Vercel** | CI/CD из коробки, бесплатно |
| Авторизация | **Telegram Login Widget** | Аудитория в Telegram |
| Проверка членства | **Telegram Bot API (getChatMember)** | Бот проверяет членство в группе |
| Видеохостинг | **YouTube (unlisted links)** | Бесплатно, iframe embed |
| Сообщество | **Telegram-группа в форум-режиме (Topics)** | Структурированное обсуждение |
| Оплата | **InviteMember** | Stripe + крипта + Telegram Stars |

---

## 4. Дизайн-система

### Цвета (согласованы с yesworkflow.com)

```css
/* Тёмный фон (как у yesworkflow) */
--background: #0d0f14;         /* основной фон */
--background-card: #161a23;    /* фон карточек */
--border: rgba(255,255,255,0.08);

/* Акцент — cyan/teal (единый бренд) */
--accent: #00C4B4;             /* основной акцент */
--accent-hover: #00A89A;
--accent-glow: rgba(0,196,180,0.15);

/* Текст */
--text-primary: #F8FAFC;
--text-secondary: #94A3B8;
--text-muted: #475569;
```

### Стиль компонентов
- **Карточки:** rounded-xl (12px), тёмный фон, тонкая граница `border-white/8`
- **Кнопки:** закруглённые (Apple-стиль), primary = teal fill, ghost = teal outline
- **Иконки:** Lucide React (используется в shadcn/ui)
- **Типографика:** Geist Sans (шрифт Vercel — современный, чистый)
- **Блокировка контента:** blur + замок (🔒), не агрессивный — деликатный

### shadcn/ui компоненты для установки
```
Button, Card, Badge, Avatar, Tabs, Dialog, Separator,
NavigationMenu, Skeleton, Toast, Sheet (mobile nav),
Progress (для курсов)
```

### Паттерн карточки ресурса (как у aizdec.me)
- Иконка категории + название
- Короткое описание
- Бейдж: FREE / MEMBER
- Для заблокированных: тусклый текст + иконка замка

---

## 5. Структура страниц (App Router)

```
app/
├── (public)/
│   ├── page.tsx                    # Landing / главная
│   ├── library/
│   │   ├── page.tsx                # Библиотека ресурсов
│   │   └── [slug]/page.tsx         # Страница ресурса
│   ├── join/page.tsx               # Вступление + InviteMember CTA
│   └── layout.tsx                  # Public layout (nav, footer)
│
├── (members)/
│   ├── layout.tsx                  # Middleware: redirect if not member
│   ├── courses/
│   │   ├── page.tsx                # Список курсов
│   │   └── [course]/
│   │       ├── page.tsx            # Список уроков (с прогрессом)
│   │       └── [lesson]/page.tsx   # Урок (видео + текст)
│   ├── community/page.tsx          # Ссылки в Telegram + анонсы эфиров
│   └── start/page.tsx              # Онбординг: с чего начать (ПЕРВЫЙ ЭКРАН)
│
├── (auth)/
│   ├── login/page.tsx              # Telegram Login Widget
│   └── callback/route.ts           # Обработка Telegram auth
│
├── api/
│   ├── auth/telegram/route.ts      # Верификация hash + getChatMember
│   └── user/role/route.ts          # Обновление роли (cron или по запросу)
│
└── layout.tsx                      # Root layout
```

---

## 6. Схема базы данных

```sql
-- Профили пользователей
CREATE TABLE profiles (
  telegram_id        BIGINT PRIMARY KEY,
  username           TEXT,
  first_name         TEXT,
  role               TEXT DEFAULT 'free',  -- 'free' | 'member'
  role_checked_at    TIMESTAMPTZ,
  created_at         TIMESTAMPTZ DEFAULT NOW()
);

-- Контент
CREATE TABLE content_items (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  slug         TEXT UNIQUE NOT NULL,
  title        TEXT NOT NULL,
  description  TEXT,
  type         TEXT,  -- 'template' | 'video' | 'prompt' | 'tool' | 'guide'
  category     TEXT,  -- 'n8n' | 'claude' | 'chatgpt' | 'vibe-coding'
  content_url  TEXT,  -- YouTube ID или внешняя ссылка
  content_body TEXT,  -- MDX/Markdown для текстового контента
  is_premium   BOOLEAN DEFAULT FALSE,
  tags         TEXT[],
  sort_order   INTEGER DEFAULT 0,
  published    BOOLEAN DEFAULT FALSE,
  created_at   TIMESTAMPTZ DEFAULT NOW()
);

-- Курсы
CREATE TABLE courses (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  slug        TEXT UNIQUE NOT NULL,
  title       TEXT NOT NULL,
  description TEXT,
  cover_url   TEXT,
  is_premium  BOOLEAN DEFAULT TRUE,
  sort_order  INTEGER DEFAULT 0,
  published   BOOLEAN DEFAULT FALSE
);

-- Модули курса
CREATE TABLE course_modules (
  id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  course_id  UUID REFERENCES courses(id) ON DELETE CASCADE,
  title      TEXT NOT NULL,
  sort_order INTEGER DEFAULT 0
);

-- Уроки
CREATE TABLE lessons (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  module_id   UUID REFERENCES course_modules(id) ON DELETE CASCADE,
  slug        TEXT NOT NULL,
  title       TEXT NOT NULL,
  youtube_id  TEXT,
  content     TEXT,  -- текст урока (Markdown)
  duration    INTEGER,  -- минуты
  sort_order  INTEGER DEFAULT 0,
  published   BOOLEAN DEFAULT FALSE
);

-- Прогресс пользователя
CREATE TABLE lesson_progress (
  telegram_id BIGINT REFERENCES profiles(telegram_id),
  lesson_id   UUID REFERENCES lessons(id),
  completed   BOOLEAN DEFAULT FALSE,
  completed_at TIMESTAMPTZ,
  PRIMARY KEY (telegram_id, lesson_id)
);
```

**RLS политики:**
- `content_items`: `is_premium = false` → все; `is_premium = true` → только members
- `lessons`: из premium курсов → только members
- `lesson_progress`: пользователь видит/пишет только свои данные

---

## 7. Авторизация (детально)

```typescript
// Поток авторизации
1. GET /login → Telegram Login Widget (JS виджет с bot_username)
2. Telegram → callback с { id, first_name, username, hash, auth_date }
3. POST /api/auth/telegram → верификация HMAC-SHA256(hash, bot_token)
4. Bot API: getChatMember(TELEGRAM_PAID_GROUP_ID, user.id)
   → member/administrator/creator → role = 'member'
   → left/kicked → role = 'free'
5. Supabase: upsert profiles + set cookie через @supabase/ssr
6. Redirect → /start (members) или / (free)

// Middleware (app/middleware.ts)
- Защищает все роуты под (members)/
- Перенаправляет неавторизованных на /login
- Перенаправляет free-пользователей на /join

// Обновление роли
- При каждом логине
- Раз в 24ч через Vercel Cron (опционально)
```

---

## 8. Безопасность credentials

**Правило: я (агент) НИКОГДА не вижу реальные ключи.**

```
.env.local        → локальная разработка, НЕ в git (.gitignore)
Vercel dashboard  → продакшн secrets, только у тебя доступ
.env.example      → только имена переменных, без значений (в git)
```

**Переменные окружения (только имена):**
```
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=
TELEGRAM_BOT_TOKEN=
TELEGRAM_PAID_GROUP_ID=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

**Telegram MCP (n8n):** не трогаем credentials — они уже настроены.
**Для разработки:** ты вводишь значения сам, локально, я работаю с именами.

---

## 9. Структура Telegram-группы (форум-режим / Topics)

Порядок топиков для новых участников:

```
📢 Объявления         — только admin, pinned. Новости, записи эфиров.
👋 Знакомства         — представься: кто ты, зачем здесь, цель.
🚀 Старт / с чего начать — закреплённый гайд первой недели.
❓ Вопросы            — любые вопросы по AI, инструментам.
🎯 Результаты         — делись прогрессом, победами (даже маленькими).
🤖 N8N                — воркфлоу, шаблоны, автоматизации.
🧠 Claude Code        — скиллы, паттерны, примеры.
💬 Chatgpt / LLM      — промты, кейсы, лайфхаки.
📅 Записи эфиров      — ссылки на YouTube + краткое содержание.
```

**Настройки группы:**
- Супергруппа с включённым Forum mode
- Бот — администратор (для getChatMember)
- Slow mode: 5 минут (против спама на старте)

---

## 10. Онбординг программа (первое что видит member)

Страница `/start` — "С чего начать":

```
НЕДЕЛЯ 1: Знакомство с инструментами
  День 1 — Зайди в Telegram-группу, напиши знакомство
  День 2 — Посмотри урок: "Что такое AI-автоматизация за 10 минут"
  День 3 — Скачай первый N8N шаблон и попробуй запустить
  День 4 — ChatGPT промты: твой первый рабочий промпт
  День 5 — Запись эфира #1: разбираем вопросы новичков

НЕДЕЛЯ 2: Первые автоматизации
  ...

→ Каждый день: поделись результатом в #результаты
```

---

## 11. MVP — минимум для запуска

- [ ] Next.js проект (create-next-app + Tailwind + shadcn/ui)
- [ ] `.env.example` с именами переменных
- [ ] Supabase: миграции схемы + RLS
- [ ] Telegram Login Widget + hash verification API route
- [ ] Bot getChatMember → role assignment
- [ ] Middleware защита (members)/ роутов
- [ ] Landing page (`/`) — кто ты, что внутри, CTA
- [ ] Library page (`/library`) — 10-15 ресурсов
- [ ] `/start` — онбординг страница для members
- [ ] `/join` — описание + кнопка InviteMember
- [ ] Деплой на Vercel

---

## 12. Архитектура контента (три типа)

### Тип 1: ИНСТРУМЕНТЫ (карточки-разделы)
Каждый инструмент — страница `/tools/[slug]` с тематическими ресурсами.

| Инструмент | slug | Статус | Что внутри |
|---|---|---|---|
| N8N | `n8n` | ✅ Запускаем | Шаблоны воркфлоу (free), промпты для агентов (members) |
| Claude Code | `claude-code` | ✅ Запускаем | Скиллы (free), паттерны (members) |
| ChatGPT | `chatgpt` | ✅ Запускаем | Промты (free + members) |
| Lovable | `lovable` | 🔒 Locked | Появится позже |
| Другое | `other` | 🔒 Locked | Видеогенерация, ClaudeBot, etc. |

### Тип 2: КУРСЫ (модули → уроки, видео)
Отдельный раздел `/courses`, нумерованный список как у aizdec.me.

| # | Курс | Статус | Есть ли контент |
|---|---|---|---|
| 1 | N8N автоматизации | ✅ Доступен | Курс записан |
| 2 | ChatGPT с нуля | ✅ Доступен | Курс записан |
| 3 | Claude Code + Вайбкодинг | 🔒 Скоро | Нет |
| 4 | Lovable | 🔒 Скоро | Нет |
| 5 | Агентные системы для бизнеса | 🔒 Скоро | Нет |
| 6 | Продвижение и продажи | 🔒 Скоро | Нет |

### Тип 3: БЫСТРЫЕ РЕСУРСЫ (отдельные файлы/шаблоны)
Не модульные — просто список скачиваемых/копируемых материалов.
Хранятся в `content_items`, отображаются на `/tools/[slug]`.

**Принцип:** locked = карточка видна, но контент недоступен. Это "удочка" без обещаний.

### База данных: поля `content_items`
```
type:  'template' | 'prompt' | 'skill' | 'guide' | 'workflow'
tool:  'n8n' | 'claude-code' | 'chatgpt' | 'lovable' | 'other'
is_premium: true/false
```

### Цена членства
- $50 / месяц
- $130 / 3 месяца

---

## 13. Скиллы для разработки

Установлены глобально в `~/.agents/skills/`:

| Скилл | Путь | Применять когда |
|---|---|---|
| `nextjs-app-router-patterns` | `~/.agents/skills/nextjs-app-router-patterns/SKILL.md` | Server Components, routing, streaming |
| `nextjs-supabase-auth` | `~/.agents/skills/nextjs-supabase-auth/SKILL.md` | Auth setup, middleware, @supabase/ssr |
| `nextjs-best-practices` | `~/.agents/skills/nextjs-best-practices/SKILL.md` | Общие паттерны, структура кода |
| `ui-design-system` | `~/.agents/skills/ui-design-system/SKILL.md` | Дизайн, компоненты, токены, dark mode |
| `supabase-postgres-best-practices` | `~/.agents/skills/supabase-postgres-best-practices/SKILL.md` | RLS, запросы, миграции |
| `telegram-bot-builder` | `~/.agents/skills/telegram-bot-builder/SKILL.md` | Telegram бот, getChatMember |

**ВАЖНО:** Перед использованием скилла — прочитать его файл полностью.

---

## 14. Модели Claude

| Задача | Модель |
|---|---|
| Архитектурные решения, планирование | **Opus 4.6** |
| Компоненты, страницы, API routes | **Sonnet 4.6** |
| SQL, RLS, миграции | **Sonnet 4.6** |
| Telegram бот логика | **Sonnet 4.6** |
| Быстрые правки стилей | **Haiku 4.5** |
| Застрявший баг | **Opus 4.6** |

---

## 15. Git + Vercel — управление и деплой

### Как это работает
```
GitHub repo (main) → Vercel читает → автодеплой в продакшн
GitHub repo (любая ветка) → Vercel → preview URL (тест перед продакшном)
```

### Структура веток (минималистичная для соло)

| Ветка | Назначение | Деплой |
|---|---|---|
| `main` | Живой сайт (продакшн) | ✅ Автодеплой на домен |
| `dev` | Текущая разработка | Preview URL (тест-ссылка) |

**Правило:** работаем в `dev`, проверяем на preview URL, мержим в `main` — сайт обновился.

### Рабочий процесс с Claude Code

```bash
# Начало новой задачи — переключиться на dev (если не там)
git checkout dev

# Claude Code пишет код → всё сохраняется локально
# Проверяешь локально (npm run dev)
# Пушишь в dev → Vercel даёт preview URL
git add . && git push origin dev

# Если всё ок → мержишь в main → продакшн обновился
git checkout main && git merge dev && git push origin main
```

### Откат если что-то сломалось

```bash
# Посмотреть последние коммиты
git log --oneline -10

# Откатиться к конкретному коммиту (не трогая main)
git revert HEAD          # отменить последний коммит
git revert <commit-hash> # отменить конкретный

# Vercel также хранит историю деплоев — можно откатить в один клик в дашборде
```

### Что нужно настроить один раз
1. Создать repo на GitHub (приватный)
2. Подключить Vercel к этому repo (кнопка "Import Project")
3. В Vercel: указать `main` как production branch
4. В Vercel: добавить все env-переменные из `.env.example`
5. Локально: `git remote add origin <repo-url>`

### .gitignore (обязательно)
```
.env.local          # НИКОГДА не коммитить
.env*.local
node_modules/
.next/
```

---

## 16. Модели Claude по этапам (с учётом лимита токенов)

**Правило:** Sonnet по умолчанию → Haiku для мелочей → Opus только когда застрял.

| Тип задачи | Модель | Примеры |
|---|---|---|
| Читаю файлы, мелкая правка | **Haiku 4.5** | CSS-правки, переименовать переменную, добавить класс |
| Новый компонент / страница | **Sonnet 4.6** | Карточка, навигация, форма |
| Логика auth / middleware / RLS | **Sonnet 4.6** | Telegram login, защита роутов |
| Архитектурный выбор | **Opus 4.6** | Редко, только для решений — не для кода |
| Сложный баг (Sonnet не справился) | **Opus 4.6** | Последнее средство |

**Экономия токенов:**
- Одна сессия = одна зона (только БД, или только Auth, или только UI)
- Не давать огромный контекст без необходимости — спрашивать точечно
- PROJECT.md восстанавливает контекст в начале каждой сессии

---

## 17. Статус по сессиям

> Обновляется в конце каждой рабочей сессии.

### Сессия 1 — 2026-02-20
**Что сделано:**
- Перенесены скиллы из Cursor в `~/.claude/commands/` (19 файлов)
- Перенесены MCP серверы в `~/.claude/settings.json`
- Установлены 4 новых скилла: nextjs-app-router-patterns, nextjs-supabase-auth, nextjs-best-practices, ui-design-system
- Создан PROJECT.md с полной архитектурой проекта

**Следующий шаг:**
- Создать GitHub repo + инициализировать Next.js проект
- Команда: `npx create-next-app@latest community-site --typescript --tailwind --app --src-dir --import-alias "@/*"`

---

## 18. Открытые вопросы

| # | Вопрос | Приоритет |
|---|---|---|
| 1 | Оплата рублями для РФ-аудитории (YooKassa не в InviteMember) | Высокий |
| 2 | Домен сайта (купить на Namecheap, подключить к Vercel) | Средний |
| 3 | Содержание онбординг-программы по неделям (детально) | Средний |
| 4 | Аналитика: Vercel Analytics (бесплатно, 1 строка кода) | Низкий |
